Number = int(input("Bir sayı girin: "))

toplam = 0
for i in range(1, Number):
   if Number % i == 0:
       toplam += i

if toplam == Number:
    print("mükemmel sayıdır")
else:
    print("mükemmel sayı değildir")

#Gizem Karaarduç ile pair çalışması.
